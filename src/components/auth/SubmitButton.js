import React from 'react'

export const SubmitButton = ({ content }) => {
    return (
        <button
            type="submit"
            className="auth__box-btnSubmit">{content}
        </button>

    )
}
